#ifndef _Reverse
#define _Reverse

#include "string"
using namespace std;

class Reverse {
    public:
    Reverse();
    int reverseDigit(int value);
    string reverseString(string letters);

};

#endif